name: praxy_sdk_gen
description: >-
  Generates praxy_core's mechanical service surface from docs/openapi/v1.json.
  Repo-internal tooling, never published: it is run by maintainers and gated in
  CI, and its output is committed like any other source file.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.12.2

resolution: workspace

dev_dependencies:
  test: ^1.25.0
