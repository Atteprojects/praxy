name: praxy_example
description: "Praxy Flutter SDK example — sign up, Google sign-in, row CRUD, a live list."
publish_to: 'none'
resolution: workspace

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  praxy_flutter: ^0.1.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
